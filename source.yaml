apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: kubernetes-repo
  namespace: flux-system
spec:
  interval: 5m
  url: https://github.com/veliValentine/kubernetes
  ref:
    branch: main