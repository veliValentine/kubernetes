apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: space-1
  labels:
    app: postgres
spec:
  ports:
  - port: 5432
    name: web
  clusterIP: None
  selector:
    app: postgres-app